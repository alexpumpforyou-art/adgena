export const metadata = {
  title: 'Генератор — AdGena',
  description: 'Создавайте карточки товара и рекламные креативы с помощью AI',
};

export default function DashboardLayout({ children }) {
  return children;
}
