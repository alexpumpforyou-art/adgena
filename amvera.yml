meta:
  environment: docker
  toolchain:
    name: docker
    version: ""

build:
  dockerfile: Dockerfile
  skip: false

run:
  containerPort: 3000
  persistentStorage:
    - mountPath: /app/data
      size: 1Gi
